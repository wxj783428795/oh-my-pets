{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Product window permissions for the macOS preview candidate",
  "windows": ["pet", "preferences"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
